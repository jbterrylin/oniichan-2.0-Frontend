const PAPER_TYPE = {
  QUOTATION: "quotation",
  INVOICE: "invoice",
};

const PRICE_UNIT = {
  RM: "rm",
  SGD: "sgd",
};

const WORD_FILTER = {
  EN: "en",
  CN: "cn",
  CAT: "category",
};

export { PAPER_TYPE, PRICE_UNIT, WORD_FILTER };
